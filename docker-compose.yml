version: '3.8'

services:
  feishu-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: feishu-api
    ports:
      - "8080:8080"
    environment:
      - GIN_MODE=release
      - PORT=8080
      - DATABASE_PATH=/tmp/feishu_api.db
    restart: unless-stopped